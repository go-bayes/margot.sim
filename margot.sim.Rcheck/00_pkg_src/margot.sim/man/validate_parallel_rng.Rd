% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/margot-rng-future.R
\name{validate_parallel_rng}
\alias{validate_parallel_rng}
\title{Validate parallel RNG reproducibility}
\usage{
validate_parallel_rng(
  test_fn,
  n_reps = 10,
  seed = 123,
  backends = c("sequential", "future", "parallel")
)
}
\arguments{
\item{test_fn}{Function to test}

\item{n_reps}{Number of replications}

\item{seed}{Seed for testing}

\item{backends}{Character vector of backends to test}
}
\value{
Data frame with reproducibility test results
}
\description{
Tests that a function produces identical results when run with
different parallel backends.
}
\examples{
\dontrun{
test_fn <- function(i) {
  sum(rnorm(100))
}

results <- validate_parallel_rng(
  test_fn = test_fn,
  n_reps = 10,
  seed = 123,
  backends = c("sequential", "future")
)
}
}
