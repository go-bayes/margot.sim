% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/margot-shadows-extended.R
\name{create_mode_effects_shadow}
\alias{create_mode_effects_shadow}
\title{Create a mode effects shadow}
\usage{
create_mode_effects_shadow(
  variables,
  mode_var,
  effect_specs,
  reference_mode = NULL,
  name = NULL
)
}
\arguments{
\item{variables}{Character vector of variable names affected by mode}

\item{mode_var}{Character name of the variable indicating collection mode}

\item{effect_specs}{List specifying effects for each mode. Each mode should
have a list with elements: shift (additive), scale (multiplicative),
and/or noise (additional error SD)}

\item{reference_mode}{Character name of the reference mode (no effect applied)}

\item{name}{Optional name for the shadow}
}
\value{
A mode effects shadow object
}
\description{
Creates a shadow that applies systematic measurement differences based on
data collection mode (e.g., phone vs in-person, self-report vs clinical).
}
\examples{
# create mode effects for survey responses
mode_shadow <- create_mode_effects_shadow(
  variables = c("income", "health_rating"),
  mode_var = "survey_mode",
  effect_specs = list(
    phone = list(shift = 0.1, scale = 0.95),     # slight underreporting
    online = list(shift = -0.05, scale = 1.05),  # slight overreporting
    paper = list(shift = 0, scale = 1, noise = 0.2)  # more random error
  ),
  reference_mode = "in_person"
)
}
