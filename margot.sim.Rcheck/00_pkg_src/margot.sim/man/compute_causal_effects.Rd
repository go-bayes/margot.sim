% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/margot-causal-effects.R
\name{compute_causal_effects}
\alias{compute_causal_effects}
\title{Compute Causal Effects from Data}
\usage{
compute_causal_effects(
  data,
  exposure,
  outcome,
  covariates = NULL,
  estimands = c("ate", "att", "atu"),
  weights = NULL
)
}
\arguments{
\item{data}{A data frame containing the outcome and exposure variables}

\item{exposure}{Character string naming the exposure/treatment variable}

\item{outcome}{Character string naming the outcome variable}

\item{covariates}{Character vector of covariate names (currently unused, for future HTE)}

\item{estimands}{Character vector specifying which estimands to compute}

\item{weights}{Optional weights for each observation}
}
\value{
A list containing the requested causal effects:
\itemize{
  \item ate: Average Treatment Effect E[Y(1) - Y(0)]
  \item att: Average Treatment on Treated E[Y(1) - Y(0) | A = 1]
  \item atu: Average Treatment on Untreated E[Y(1) - Y(0) | A = 0]
}
}
\description{
Calculates various causal estimands (ATE, ATT, ATU) from simulated or observed data.
This function assumes the data contains potential outcomes or can be used to compute
contrasts between treatment groups.
}
\examples{
# Generate simple data
set.seed(123)
n <- 1000
data <- data.frame(
  a = rbinom(n, 1, 0.5),
  y = rnorm(n, mean = 2)
)
data$y[data$a == 1] <- data$y[data$a == 1] + 0.5  # True ATE = 0.5

# Compute effects
effects <- compute_causal_effects(data, exposure = "a", outcome = "y")
print(effects)

}
