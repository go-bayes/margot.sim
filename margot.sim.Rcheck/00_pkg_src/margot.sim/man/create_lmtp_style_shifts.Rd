% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/margot-shift-interventions.R
\name{create_lmtp_style_shifts}
\alias{create_lmtp_style_shifts}
\title{Example shift interventions matching lmtp style}
\usage{
create_lmtp_style_shifts(
  min_score = 1,
  max_score = 7,
  baseline_wave = 0,
  shift_wave = 1
)
}
\arguments{
\item{min_score}{Minimum score value}

\item{max_score}{Maximum score value}

\item{baseline_wave}{Wave to keep as observed (typically 0)}

\item{shift_wave}{Wave to apply shift (typically 1)}
}
\value{
List containing shift_up and shift_down intervention functions
}
\description{
Example shift interventions matching lmtp style
}
\examples{
# Create lmtp-style shift interventions
shifts <- create_lmtp_style_shifts(
  min_score = 1,
  max_score = 7,
  baseline_wave = 0,
  shift_wave = 1
)

# Use in margot_simulate_causal
\dontrun{
results <- margot_simulate_causal(
  n = 1000,
  waves = 2,
  treatments = "a",
  interventions = list(
    natural = function(data, time, trt) data[[trt]],
    shift_up = shifts$shift_up,
    shift_down = shifts$shift_down
  )
)
}
}
