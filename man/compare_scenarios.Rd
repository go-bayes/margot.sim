% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/margot-scenarios.R
\name{compare_scenarios}
\alias{compare_scenarios}
\title{Compare causal effects across scenarios}
\usage{
compare_scenarios(
  data,
  scenarios,
  exposure,
  outcome,
  estimands = c("ate", "att", "atu"),
  verbose = FALSE
)
}
\arguments{
\item{data}{Data frame with the true data}

\item{scenarios}{List of scenario objects to compare}

\item{exposure}{Character, name of exposure variable}

\item{outcome}{Character, name of outcome variable}

\item{estimands}{Character vector of estimands to compute}

\item{verbose}{Logical, whether to print progress}
}
\value{
A scenario comparison object containing effects under each scenario
}
\description{
Compares the same causal effect estimate across different scenarios to
assess sensitivity to assumptions.
}
\examples{
# create data
sim_data <- margot_simulate(n = 1000, waves = 2)

# define scenarios
scenarios <- list(
  oracle = create_scenario("Oracle", shadows = list()),
  realistic = create_scenario(
    "Realistic",
    shadows = list(
      create_shadow(
        type = "measurement_error",
        params = list(
          variables = "t1_a",
          error_type = "misclassification",
          sensitivity = 0.85,
          specificity = 0.90
        )
      )
    )
  )
)

# compare scenarios
comparison <- compare_scenarios(
  sim_data,
  scenarios,
  exposure = "t1_a",
  outcome = "t2_y"
)

print(comparison)
}
